// Globals

// __dirname => gives the path to the current directory
// __filename => gives the filename and the path
// module => gives information about the current module
// process => gives info about the env in which the program is running
// require => used to work with modules

console.log(__dirname);
console.log(__filename);
console.log(require);